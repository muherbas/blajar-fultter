name: statistik_atlet_app
description: "A new Flutter project for Athlete Dashboard Statistics."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Menyesuaikan dengan versi Flutter yang Anda gunakan

dependencies:
  flutter:
    sdk: flutter

  # Tidak perlu menambahkan dependency chart eksternal karena grafik 
  # digambar manual menggunakan Canvas & CustomPainter bawaan Flutter.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
