name: belajar_boxplot
description: Aplikasi untuk belajar grafik Boxplot dengan Flutter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Menambahkan package pembuat grafik
  syncfusion_flutter_charts: ^24.2.7
  syncfusion_flutter_gauges: ^24.2.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
